// import { fromJS } from 'immutable';
// import { makeSelectSubmissionPageDomain } from '../selectors';

// const selector = makeSelectSubmissionPageDomain();

describe('makeSelectSubmissionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
