// import { fromJS } from 'immutable';
// import { makeSelectGradingPageDomain } from '../selectors';

// const selector = makeSelectGradingPageDomain();

describe('makeSelectGradingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
